/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package pkg00390aw;


import java.io.*;
import java.net.*;

/**
 *
 * @author WINDOWS 10
 */
public class Main {

    /**
     * @param args the command line arguments
     */
    
    protected static ServerSocket ss;
    protected static Socket s;
    protected static DataOutputStream salidaServidor,salidaCliente;
    public static void main(String[] args) throws IOException {
        String [][] s=new String[2][7];
        System.out.println(s[-1].length);
        
       /* ListaReservas a=new ListaReservas();
        Reservas e=new Reservas();
        e.setNombre("a");
        a.getReservas().add(e);
        
        e=new Reservas();
        e.setNombre("b");
        a.getReservas().add(e);
        System.out.println(a.getReservas().remove().getNombre());
        System.out.println(a.getReservas().remove().getNombre());*/
    }
    
}
